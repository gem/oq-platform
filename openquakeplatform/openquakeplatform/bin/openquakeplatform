#!/bin/bash
if hash django-admin 2>/dev/null; then
    django-admin $@ --settings=openquakeplatform.settings
else
    django-admin.py $@ --settings=openquakeplatform.settings
fi
# FIXME
sleep 2000000 || true
